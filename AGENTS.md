# AGENTS.md

This file is the agent entry point for `personal/teach/markdown_convert/`.

## README First

- Read `README.md` first. It is the Google Docs add-on feature, install, and usage source.
- Keep supported syntax, installation steps, and usage details in `README.md`.
- Keep current add-on notes in `memory.md`; keep old add-on history in `archive.md`.

## Agent Rules

- Use simple English for docs and code.
- Treat this as a Google Apps Script add-on project.
- Do not publish or deploy the add-on unless the user asks.
- Keep account-specific IDs, tokens, and deployment details out of commits.
- Run Git commands from this folder.
